# GJB 2874-97 IV-A 采样参数审计

本文只回答一个问题：当前 `pingmo` 的 P-channel 采样量中，哪些能直接映射到
GJB 2874-97 的 IV 类、A 种飞行阶段建议边界，哪些不能。本文不修改采样器，也不把
附录建议值解释为真实机群的概率分布。

## 1. 口径和限制

- GJB 正文保留了任务、类别、飞行阶段和数值剪裁空格；附录 A 明确是参考件。下文所称
  “Level 1/2/3 边界”均是附录 A 的建议值，不是脱离型号任务合同后仍普遍有效的
  “允许采样区间”。
- 当前记录只声明 `aircraft_class=IV`、`flight_phase=A`，没有保存 A 阶段中的
  `CO/GA` 任务子类、操纵器类型、速度、高度、构形、质量、重心等条件。因此部分边界
  只能条件式报告，不能给每架样本唯一的正式 GJB 等级。
- 当前连续参数为
  `theta=[L_Fa, lambda_s, T_R, zeta_d, omega_d, R_omega, R_zeta, tau_p]`；另外先采
  `S_1s`，再反标 `L_Fa`，并派生 `omega_phi` 和 `zeta_phi`。
- 本审计针对当前单 `s` 候选模型：

```math
\frac{p}{F_{as}}=
\frac{L_{F_a}s(s^2+2\zeta_\phi\omega_\phi s+\omega_\phi^2)}
{(s-\lambda_s)(s+1/T_R)(s^2+2\zeta_d\omega_d s+\omega_d^2)}
e^{-\tau_p s}.
```

## 2. GJB 能直接约束的量

| 项目量 | GJB IV-A 建议边界 | 映射结论 | 原始页 |
| --- | --- | --- | --- |
| `T_R` | Level 1 `<=1.0 s`；Level 2 `<=1.4 s`；Level 3 `<=10 s` | 直接映射，但只有上限，没有 GJB 采样下限 | 表 A18，PDF 215／印刷 211 |
| `lambda_s` | A/C 发散螺旋倍幅时间 `T_2`：Level 1 `>=12 s`；Level 2 `>=8 s`；Level 3 `>=4 s` | 对 `lambda_s>0`，等价上限分别为 `ln(2)/12=0.057762`、`ln(2)/8=0.086643`、`ln(2)/4=0.173287 1/s`；对 `lambda_s<=0`，A19 不给负向数值范围 | 表 A19，PDF 229／印刷 225 |
| `zeta_d, omega_d` | 普通 IV-A Level 1：`zeta_d>=0.19`、`zeta_d*omega_d>=0.35 rad/s`、`omega_d>=1.0 rad/s` | 必须联合判断，不能分别当成独立允许区间 | 表 A35，PDF 303／印刷 299 |
| `zeta_d, omega_d`，IV-A 的 CO/GA 子任务 | Level 1：`zeta_d>=0.4`、`zeta_d*omega_d>=0.4 rad/s`、`omega_d>=1.0 rad/s` | 比普通 IV-A 更严格；当前样本缺少 CO/GA 标签 | 表 A35，PDF 303／印刷 299 |
| `zeta_d, omega_d`，所有类别/阶段 | Level 2：`zeta_d>=0.02`、`zeta_d*omega_d>=0.05 rad/s`、`omega_d>=0.4 rad/s`；Level 3：`zeta_d>=0`、不限制乘积、`omega_d>=0.4 rad/s` | 可作宽松等级边界；Level 3 的 `zeta_d=0` 不等于正常机群应采零阻尼 | 表 A35，PDF 303／印刷 299 |
| `S_1s=abs(phi(1 s))/abs(F_step)` | 杆式操纵 IV 类、A 阶段：Level 1 `<=3.38 deg/N`；Level 2 `<=5.62 deg/N`；未给 Level 3 数值 | 直接约束一秒滚转操纵灵敏度，不直接约束 `L_Fa`；还需确认操纵器和试验定义 | 表 A31，PDF 276／印刷 272 |
| 最终端到端 `tau_e` | Level 1 `<=0.10 s`；Level 2 `<=0.20 s`；Level 3 `<=0.25 s` | 只约束从完整系统响应拟合出的等效延迟 | 表 A21，PDF 250／印刷 246 |

表 A35 还规定：当 `omega_d^2 * abs(phi/beta)_d > 20 rad/s` 时，要在表中
`zeta_d*omega_d` 最小值上增加与该耦合量有关的修正。当前 SISO P-channel 没有
`beta`，所以即使三个基础数值均过线，也不能完成这项修正检查。

## 3. GJB 没有给独立上下限的量

| 项目量 | GJB 中的处理方式 | 结论 |
| --- | --- | --- |
| `L_Fa` | 出现在滚转 LOES 增益中；标准另以滚转性能、操纵力和 `S_1s` 约束整体响应 | 没有通用的 `L_Fa` 数值上下限，不能把文献机型包络称作 GJB 范围 |
| `R_omega=omega_phi/omega_d` | 影响滚转-侧滑极零关系；A5.4.1.1.4 最终以阶跃响应的 `P_osc/P_av` 和相角评价 | 没有可独立采用的 GJB 区间 |
| `R_zeta=zeta_phi/zeta_d` | 同样通过极零位置和完整响应起作用 | 没有可独立采用的 GJB 区间 |
| `omega_phi` | 分子零点参数；与 `omega_d` 的相对位置影响 A116 响应 | 没有通用独立上下限 |
| `zeta_phi` | 分子零点参数；与 `zeta_d` 共同决定 A116 响应 | 没有通用独立上下限 |
| 当前 `tau_p` | 代码实现为输入前的纯运输 FIFO 延迟 | 不等于表 A21 的拟合 `tau_e`，因此全部样本对 A21 都应记为 `not_assessable`，而不是按 `tau_p` 数值打等级 |

对 `R_omega/R_zeta/omega_phi/zeta_phi`，本项目能做的 GJB 启发式检查是图 A116/A120：
先满足滚转、螺旋和荷兰滚前置条件，再由小阶跃响应得到 `P_osc/P_av` 与
`psi_beta`；当前无 `beta` 状态，只能使用标准讨论中允许的 `psi_p` 替代量。这是组合响应
边界，不是四个参数各自的矩形范围（图 A116：PDF 240／印刷 236；图 A120：PDF 244／
印刷 240）。A 阶段还不允许出现滚转-螺旋耦合振荡（表 A20，PDF 232／印刷 228）。

## 4. 抽样机制和控制实验常数

这些量会影响机库或后续实验，但不属于八维 `theta`，也不能混入“GJB 允许参数范围”。

| 量 | 当前实现 | GJB 映射 |
| --- | --- | --- |
| 区域选择潜变量 `q=unit[0]` | 候选池按 Sobol 坐标划为 core `50%`、boundary `26%`、OOD `15%`、extreme `9%` | `q` 不是动态压强，只是项目自定义的无量纲分区变量；GJB 不规定统计概率 |
| 最终机库配额 | core `1950`、boundary `600`、OOD `300`、extreme `150`，分别占 `65%/20%/10%/5%` | 项目数据集设计；不代表真实机群分布 |
| Sobol 配置 | seed `20260827`，候选数 `16384`，从中取 `3000` 个有效样本 | 复现参数；无 GJB 品质等级含义 |
| 飞行包线条件 | 未采 `V/h/Mach/alpha/n_z`、构形、质量和重心 | 这些条件本应随样本保存；当前缺失意味着这里只是低阶传函族，不是完整飞机工况族 |
| 驾驶员输入尺度 | 当前命令以 `22 N` 为名义满幅 | 表 A30 对 IV-A 中央杆给出的最大力为 Level 1 `88.96 N`、Level 2 `133.50 N`、Level 3 `155.68 N`；相应建议最小增量是“启动力 + 表值的 `1/4`、`1/8`、`0`”。`22 N` 只近似 Level 1 的 `88.96/4=22.24 N` 增量，既未计启动力，也不是 GJB 指定的阶跃幅值。轮式最大力为 `177.92/266.88/311.36 N`；侧杆没有建议值（表 A30，PDF 275-276／印刷 271-272） |
| RL 修正权限 | `correction_ratio=0.3`，即名义 `22 N` 下 `±6.6 N` | 项目控制权限；GJB 没有规定这个比例 |
| 修正动作限速 | 归一化 `4 s^-1`，对应当前尺度 `26.4 N/s` | 项目约束；不是 GJB 参数范围 |
| 修正通道一阶时间常数 | `0.08 s` | 项目探索值；既不是采样周期，也不是 GJB 的拟合 `tau_e` 上限 |
| 数值时基 | 策略 `0.02 s`（50 Hz），对象 `0.005 s`（200 Hz） | 仿真实现选择；不是 GJB 给定的延迟或采样范围 |

因此，“所有参数都在 GJB 允许范围内”不是当前模型可以成立的命题。能做的是：对有直接条款的
联合动态和响应逐项打标签；对其余量记录来源、设计理由和压力测试用途。

## 5. 当前代码实际提议的采样范围

以下范围来自 `src/aircraft/sampler.py::_iv_a_candidate`，是项目设计，不是 GJB 允许范围。

| 量 | core | boundary | OOD | extreme |
| --- | --- | --- | --- | --- |
| `S_1s` (`deg/N`) | `0.85-5.62` | `2.7-6.0` | `0.4-8.43` | `0.4-8.43` |
| `T_R` (`s`) | `0.18-1.40` | `0.12-0.35` 或 `0.85-1.70` | `0.08-3.0` | `0.08-10.0` |
| `lambda_s` (`1/s`) | `-0.15-0.0578` | `0.045-0.10` | `-0.30-0.173` | `0.173-0.30` |
| `zeta_d` | `0.15-0.70` | `0.05-0.25` | `0.02-1.0` | `0.02-0.10` |
| `omega_d` (`rad/s`) | `1.5-6.0` | `0.8-8.0` | `0.5-10.0` | `0.5-10.0` |
| `R_omega` | `0.70-1.40` | `0.45-2.20` | `0.30-0.45` 或 `2.20-3.0` | 同 OOD |
| `R_zeta` | `0.50-2.0` | `0.25-3.0` | `0.15-4.0` | `0.15-4.0` |
| `tau_p` (`s`) | `0-0.18` | `0.08-0.27` | `0.25-0.40` | `0.40-0.50` |

代码随后把 `omega_phi` 裁到 `0.1-10 rad/s`、把 `zeta_phi` 裁到 `0.03-1.5`，再反算
两个比值。`L_Fa` 没有硬上限，而是用单个 `S_1s` 目标反标。

## 6. 当前 3,000 样本的静态对照

本节只做代数边界检查，没有运行 A116 响应审计或任何训练。快照为
`GJB_s1_corrected`、seed `20260827`，`plants.jsonl` SHA-256 为
`5e1709da5e0ada055701e32eba3917fe00050b5772a947d922cece032bacd26f`。

| 可直接检查的条件 | Level 1 | Level 2 | Level 3 | 其它 |
| --- | ---: | ---: | ---: | ---: |
| `T_R` | 2290 | 503 | 207 | 0 |
| A/C 发散螺旋 | 718 | 336 | 200 | 1596 非发散；150 差于 Level 3 |
| 普通 IV-A 荷兰滚基础三条件 | 2111 | 854 | 35 | 0 |
| IV-A CO/GA 荷兰滚基础三条件 | 1210 | 1755 | 35 | 0 |
| 杆式 IV-A `S_1s` | 1909 | 981 | 未规定 | 110 高于 Level 2 上限 |

这些是各条款的独立计数，不能相加，也不构成 3,000 架飞机的综合 GJB 等级。`tau_p`、
A116 耦合、高耦合荷兰滚修正、任务/包线条件和有人评价均未包含。

当前快照的实际参数极值为：

| 量 | 最小值 | 最大值 |
| --- | ---: | ---: |
| `L_Fa` | 0.009428 | 219.568126 |
| `lambda_s` (`1/s`) | -0.299503 | 0.299026 |
| `T_R` (`s`) | 0.080258 | 9.670348 |
| `zeta_d` | 0.020400 | 0.999936 |
| `omega_d` (`rad/s`) | 0.503784 | 9.938766 |
| `R_omega` | 0.300168 | 2.995907 |
| `R_zeta` | 0.151578 | 3.966421 |
| `tau_p` (`s`) | 0.000116 | 0.499665 |
| `omega_phi` (`rad/s`) | 0.154814 | 10.000000 |
| `zeta_phi` | 0.030000 | 1.500000 |
| `S_1s` (`deg/N`) | 0.401316 | 8.399143 |

`L_Fa` 最大值达到 `219.57`，不是 GJB 允许的结果，而是高延迟/强失配样本在 1 秒积分
响应接近抵消时，单点反标把增益病态放大的结果。该问题必须在认可新机库前解决。

## 7. 审计结论和待定决策

1. GJB 边界应用于生成后的联合动态和响应，不能把每一维独立上下限的笛卡尔积称为
   “GJB 允许机群”。
2. 当前 `core` 不是 Level 1 集合：它有意包含 Level 2 的 `T_R`、荷兰滚和 `S_1s` 样本。
3. 当前 `extreme` 也不是统一的“差于 Level 3”集合；只有其发散螺旋范围全部位于 A19
   Level 3 边界之外，其余参数仍可能单项达到 Level 1/2/3。
4. 必须先确定 IV-A 是否还要细分 CO/GA，以及是否固定为杆式操纵；否则 A35 Level 1 和
   A31 无法唯一应用。
5. `tau_p` 应改名并记录为 `transport_delay_s`；另建端到端 LOES 拟合结果 `tau_e`。
6. `R_zeta` 不应因为便于采样就被称为 GJB 参数范围。更合理的方案是依据外部实机证据
   条件采 `zeta_phi`，派生 `R_zeta`，最终用 A116 响应边界筛选。
7. `L_Fa` 不能继续只靠一个可能接近零的 `S_1s` 单点反标且无物理上限。应在下一轮设计中
   决定是优先采有证据的准稳态 `K_p/F` 后派生 `L_Fa`，还是保留 `S_1s` 但增加条件数、
   可达性和增益证据门。
8. 在上述口径获确认前，不重采样、不重跑全机群审计，也不启动 SAC。
