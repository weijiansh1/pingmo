# 2026-08-28 分层覆盖 MLP-SAC 短训

## 结论

本次有效试验仍未通过。Level 1/2/3 均衡训练库上的 15 个 validation 飞机-指令配对中，
13 个配对的 episode 代价高于 Raw，严格 `harm_rate=86.67%`。只有 Level 2 和 Level 3
的 doublet 各有一个配对改善，因此不启动 MoE-SAC。

相较此前 6,000 transition、4 个 episode 的试验，本次策略明显更接近“低干预”：中位额外
代价由 `+0.021996` 降至 `+0.001036`，动作 RMS 由 `0.5152 N` 降至 `0.1282 N`，动作
总变差由 `6.3946 N` 降至 `1.5246 N`。这说明扩大覆盖有效，但尚未证明 SAC 能稳定改善
飞行品质。

## 数据与代码证据

- Git 提交：`dd5621e`；
- 数据库 schema/model：`5.0 / GJB_s1_corrected`；
- `plants.jsonl` SHA-256：`479c16d7b5ddb363c0e2d069a7a1e2d510f0a77cf8335eb86da6bb359ca4b118`；
- `manifest.json` SHA-256：`ad76514cdfeb3e34080b9996d9b485f4166333ec0ddce14abf66e7976fcb0482`；
- 训练源：Level 1/2/3 各 600 架；
- validation 源：Level 1/2/3 各 100 架；
- 实际完成 episode：Level 1/2/3 分别为 43/43/42，共 128 架且无重复；
- 14 个训练指令全部覆盖。

训练入口现在强制检查 manifest、采样策略和三级数量均衡，并把数据库及 manifest 哈希写入
checkpoint/report。旧版 `core/boundary` 数据会在创建网络和占用 GPU 前直接失败。

## 有效试验配置

| 项目 | 数值 |
| --- | ---: |
| Teacher 参数量 | 68,329,861 |
| 并行环境 | 32 |
| 对象/策略步长 | 0.001 s |
| episode 时长 | 1.25 s |
| transition | 160,000 |
| 随机预热 | 40,000 |
| batch | 256 |
| 更新间隔 | 每 32 transition 一次 |
| 梯度更新 | 3,750 |
| replay 容量 | 160,000 |
| 训练时间 | 559.52 s |
| 批量评测时间 | 91.15 s |
| GPU 峰值 allocated/reserved | 1.290/1.520 GiB |

批量评测把上一轮的 628.87 s 降到 91.15 s，约快 6.9 倍。训练时 Tesla P4 的 GPU
利用率通常为 83%--90%，没有 OOM、NaN 或动作饱和。

## 留出评测

| 指标 | 结果 | 判断 |
| --- | ---: | --- |
| 严格 harm rate | 86.67%（13/15） | 不通过 |
| episode 代价变化中位数 | +0.001036 | 变差 |
| Raw-good harm rate | 100%（2/2） | 不通过 |
| Raw-good 动作 RMS 均值 | 0.1267 N | 已降低但非零 |
| 响应起始延迟变化中位数 | +0.0005 s | 轻微变差 |
| `S1s` 变化中位数 | +0.000823 deg/N | 轻微变差 |
| 振荡比变化中位数 | +0.001222 | 变差 |
| 释放后滚转 RMS 变化中位数 | -0.001421 rad/s | 轻微改善 |
| 动作 RMS 均值 | 0.1282 N | 无饱和 |
| 动作总变差均值 | 1.5246 N | 明显低于上一轮 |

按飞机等级分解：

| 等级 | 配对 | 受害配对 | 代价变化中位数 |
| --- | ---: | ---: | ---: |
| Level 1 | 5 | 5 | +0.000960 |
| Level 2 | 5 | 4 | +0.004538 |
| Level 3 | 5 | 4 | +0.001036 |

按指令分解：

| 指令 | 配对 | 受害配对 | 代价变化中位数 |
| --- | ---: | ---: | ---: |
| step | 6 | 6 | +0.005540 |
| doublet | 3 | 1 | -0.001914 |
| sine | 3 | 3 | +0.002525 |
| chirp | 3 | 3 | +0.001036 |

严格 harm rate 对任何正代价都计为伤害，因此这里同时保留连续代价。15 个配对中有 8 个的
额外代价大于 `0.001`；该数值只用于观察量级，不是新增的通过阈值。最大变差为
`+0.010848`，最大改善为 `-0.004950`。

## 旧数据事故

第一次 160,000 transition 覆盖试验使用了 GPU 主机遗留的旧 `core/boundary` 数据库。
其 `plants.jsonl` SHA-256 为
`417d79aa9ffda8118665179123903661248c09bb61b16c9ab868779017a19d6e`，且没有 manifest。
该运行只证明并行训练吞吐，不作为 Level 1/2/3 控制效果证据。其目录保留为：

```text
/data/coding/pingmo/results/short_teacher_trials/mlp-coverage-seed-20260828-r1/
```

有效运行目录为：

```text
/data/coding/pingmo/results/short_teacher_trials/mlp-level-balanced-seed-20260828-r1/
```

## 当前判断

覆盖扩展使策略从“明显乱动”转向“多数时候接近零修正”，并在两个 doublet 配对上出现小幅
改善；但 step、sine、chirp 全部受害，Raw-good 保护仍未通过。当前证据支持继续训练 MLP，
不支持进入 MoE。

下一轮应保持数据库哈希、网络、奖励和物理权限不变，将预算提高到 320,000 transition、
256 个完整 episode 和 8,750 次更新，并把 validation 扩到每级 3 架、共 45 个配对。只有
严格 harm rate、连续代价和 Raw-good 动作同时继续改善，才值得讨论 reward 调整或 MoE。
